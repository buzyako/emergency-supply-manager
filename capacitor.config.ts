import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.emergency.supply.manager',
  appName: 'Emergency Supply Manager',
  webDir: 'www'
};

export default config;
